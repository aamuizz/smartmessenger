<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amtechnology.smartreplyassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconmain"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconmain"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Database.replystore"></activity>
        <activity android:name=".ChatActivity.ChatActivity" />
        <activity android:name=".Login.Verify_phone" />
<!--         <activity android:name=".ChatActivity.MainActivity" />-->
        <activity android:name=".ChatActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>