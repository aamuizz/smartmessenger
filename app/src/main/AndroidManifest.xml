<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amtechnology.smartreplyassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconmain"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconmain"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".replystore"></activity>
        <activity android:name=".ChatActivity" />
        <activity android:name=".Verify_phone" /> <!-- <activity android:name=".MainActivity" /> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>